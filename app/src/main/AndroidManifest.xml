<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.Cinema"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".Screen.profil"
            android:exported="false" />
        <activity
            android:name=".Screen.film"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Cinema">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Screen.signup" />
        <activity android:name=".Screen.login" />
        <activity android:name=".Screen.home" />
        <activity android:name=".Screen.ticket" />
        <activity android:name=".Screen.SeatsScreen" />
        <activity android:name=".Screen.register_ticket" />
        <activity android:name=".Screen.payment" />
    </application>

</manifest>